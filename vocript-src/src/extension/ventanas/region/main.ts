/**
 * Lo que monta esta ventana cuando NO hay extensión: nada.
 *
 * Existe para que el alias `@/extension-ventanas` resuelva siempre y el chequeo de tipos no
 * dependa de tener una extensión clonada. En la práctica no llega a ejecutarse: sin
 * extensión, esta página ni siquiera entra en la compilación.
 */
export {};
