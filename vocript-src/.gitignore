# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

package-lock.json

node_modules
dist
dist-ssr
*.local
*.local.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude/

/target/
recording_*
.crush/

# Playwright
playwright-report/
test-results/
blob-report/

.direnv
.envrc

# Nix build output
result

# Bundled speech models (.bin son grandes; los descarga el bot, no se commitean)
src-tauri/resources/models/*.bin

# Build de la demo web (se regenera con pnpm run build:demo)
dist-demo

# Catalogo de modelos extraido de src-tauri/src/managers/model.rs.
# Es un derivado: lo regeneran los scripts demo y build:demo.
demo/src/simulador/modelos.generado.json

# El motor de VoCript Pro. Vive en su propio repositorio privado y se clona aqui dentro;
# este repositorio es publico y ese codigo no lo es. Sin la carpeta, la compilacion normal
# funciona igual: solo se compila con la bandera `pro`.
/src-tauri/src/pro/
